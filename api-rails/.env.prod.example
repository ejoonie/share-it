POSTGRES_PASSWORD=
DATABASE_URL=postgres://postgres:CHANGE_ME@db:5432/sp_api_production

# FCM 발송용 서비스 계정 키 경로. docker-compose.prod.yml이 이 경로로 파일을
# bind mount하므로, 컨테이너 내부 경로(기본값)와 맞춰야 한다.
FIREBASE_SERVICE_ACCOUNT_PATH=/rails/config/firebase/service_account.json
